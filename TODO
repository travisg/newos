Implement:

PORTS:
 ppc port (iMac)
 mips port (SGI Indy)
 sparc v8 port
 sparc v9 port
 alpha port (DEC Multia)
user-space loader
disk based filesystem
kernel ports/pipes (IPC)
arguments to new processes
process environment variables
dreamcast drivers:
 maple bus layer
 keyboard
 mouse
 cdrom
sh4 cache enable - dreamcast
statistic gathering:
 process
 thread
SMP mailbox check during spin
scheduler updates:
 better quantum handling
 priority boost on sem block
improved kernel debugger support:
 symbol lookups
 stack tracing
rtl8139 driver - dreamcast
TCP/IP stack
kernel slab allocator
vfs:
 vnode caching
 getcwd
 attributes (?)
 CLOEXEC
VM:
 cache layer

In Progress:

signals
tty layer
kernel module loading:
 modules
VM:
 swap
 page out
RLD
 lazy binding
 so initialization
 dl_open() et load_addon()


Finish:

sh4 console - dreamcast

Fix:
