Implement:

PORTS:
 ppc port (iMac)
 mips port (SGI Indy)	
 sparc v8 port
 sparc v9 port
 alpha port
user-space loader
disk based filesystem
tty layer:
 maybe handle in input_server?
kernel ports/pipes (IPC)
arguments to new processes
process environment variables
dreamcast drivers:
 maple bus layer
 keyboard
 mouse
 cdrom
sh4 cache enable - dreamcast
statistic gathering:
 process
 thread
SMP mailbox check during spin
scheduler updates:
 better quantum handling
 priority boost on sem block
improved kernel debugger support:
 symbol lookups
 stack tracing
rtl8139 driver - dreamcast
TCP/IP stack
kernel slab allocator
vfs:
 vnode caching
 getcwd
 attributes (?)
VM:
 swap
 page out
 cache layer

In Progress:
kernel module loading:
 symbols
 modules

Finish:

sh4 console - dreamcast
sh4 test_and_set - dreamcast
sh4 invalidate_TLB - dreamcast
ide driver cleanup

Fix:
